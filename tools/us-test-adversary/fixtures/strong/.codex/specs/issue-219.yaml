issue: 219
goal: Reject weak tests before PR readiness.
examples:
  - id: meaningful-test
    name: traced test asserts behavior
    given:
      - a traced Rust test exists
    when:
      - the adversary gate evaluates it
    then:
      - the gate accepts the test
acceptance_criteria:
  - meaningful traced tests pass
non_goals:
  - mutation testing
architecture_impacts:
  - none
test_trace_ids:
  - meaningful-test:tests/meaningful_test.rs::meaningful_test_has_assertion
