id: no-unwrap-in-production
language: rust
severity: error
message: "unwrap() is forbidden in production code. Use ? or match instead."
rule:
  pattern: $X.unwrap()
  not:
    inside:
      stopBy: end
      kind: mod_item
